/*
var pattern = Trianglify({
    width: window.innerWidth,
    height: window.innerHeight,
    variance: "1",    
    x_colors: 'Blues',
    cell_size: 40
        
});

var banner = document.getElementById('banner')
banner.appendChild(pattern.svg());

$(document).ready(pattern);
*/